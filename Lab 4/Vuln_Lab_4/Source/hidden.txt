Admins can search user data by keyword. Seems simple enough, unless you know what the tool does behind the scenes...
But are you an admin ? Not at all. You don't even know what is different between "admin" and "admin " right ?
